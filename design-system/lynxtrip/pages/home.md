# Home Page Overrides

> **PROJECT:** LynxTrip
> **Generated:** 2026-04-18 16:03:28
> **Page Type:** Dashboard / Data View

> ⚠️ **IMPORTANT:** Rules in this file **override** the Master file (`design-system/MASTER.md`).
> Only deviations from the Master are documented here. For all other rules, refer to the Master.

---

## Page-Specific Rules

### Layout Overrides

- **Layout:** App Shell（顶部常驻 Header + 左侧导航 Sidebar + 右侧内容 Content）
- **Header:** `position: sticky; top: 0;`（滚动时常驻）
- **Sidebar:** 固定宽度 240px，随内容区滚动；菜单项突出主品牌橙
- **Content:** 宽内容区，卡片化信息架构（少阴影、重分割线与留白）

### Spacing Overrides

- **Content Density:** Low — focus on clarity

### Typography Overrides

- No overrides — use Master typography

### Color Overrides

- **Strategy:** 以 `#FF8839` 为主色；hover/active `#FF7A1A`；卡片底/hover 用 `#FFFBF0`、描边用 `#FFB07A`；背景可用 `#FFF7F1`

### Component Overrides

- No overrides — use Master component specs

---

## Page-Specific Components

- No unique components for this page

---

## Recommendations

- Effects: 扁平化；尽量不使用重阴影/大渐变；hover 用颜色/背景/边框变化；过渡 150-250ms；图标统一（不使用 emoji）
- CTA Placement: Center, large CTA button
